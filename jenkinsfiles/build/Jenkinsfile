pipeline {
  agent any
  environment {
    GIT_SSH_COMMAND = 'ssh -i ~/.ssh/ets-gpkg12'
  }
  tools {
    maven 'mvn'
    jdk 'JDK 8'
  }
  stages {
    stage('Preparation') {
      steps{
        deleteDir()
        sh 'git clone git@github.com:opengeospatial/ets-gpkg12.git .'
      }
    }
    stage('Build') {
      steps{
        sh 'mvn --version'
        sh 'mvn clean install site'
      }
    }
    stage('Results') {
      steps{
        junit '**/target/surefire-reports/TEST-*.xml'
        archive 'target/*'
      }
    }
  }
}